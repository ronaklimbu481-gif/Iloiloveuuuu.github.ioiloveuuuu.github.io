<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>For Kabita 💗</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #ffb6c1, #ffc1cc);
      font-family: 'Arial', sans-serif;
      color: #fff;
      text-align: center;
      overflow: hidden;
    }
    h1 {
      margin-top: 50px;
      font-size: 36px;
    }
    p {
      font-size: 20px;
      margin: 10px;
    }
    .hearts {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      z-index: -1;
      overflow: hidden;
    }
    .heart {
      position: absolute;
      color: rgba(255, 255, 255, 0.8);
      animation: float 5s infinite ease-in-out;
      font-size: 24px;
    }
    @keyframes float {
      0% {transform: translateY(100vh);}
      100% {transform: translateY(-10vh);}
    }
  </style>
</head>
<body>
  <h1>I love uuuu Kabita 💗</h1>
  <p>You are the reason behind my smile every day 💗</p>
  <p>My world feels perfect whenever you are with me 💗</p>
  <p>With you, every moment is a fairytale come true 💗</p>
  <p>I love you endlessly, now and forever 💗</p>
  
  <div class="hearts"></div>

  <script>
    // Floating hearts animation with 💗
    const heartsContainer = document.querySelector('.hearts');
    for (let i = 0; i < 20; i++) {
      const heart = document.createElement('div');
      heart.classList.add('heart');
      heart.innerHTML = '💗';
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.animationDuration = (3 + Math.random() * 3) + 's';
      heartsContainer.appendChild(heart);
    }
  </script>
</body>
</html>
